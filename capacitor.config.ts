import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webworms.todoAppTest1',
  appName: 'todo-app',
  webDir: 'dist/todo-app',
  bundledWebRuntime: false
};

export default config;
